# Program to calculate the area of a rectangle
def rectangle_area(length, width):
    area = length * width
    return area

# Example usage:
length = 5
width = 3
print(f"Area of rectangle with length {length} and width {width} is: {rectangle_area(length, width)}")
